name: reverb
description: "Far-field recording in a reverberant room via acoustic room simulation (1–3 m mic distance)."
transforms:
  - type: RoomSimulator
    parameters:
      min_size_x: 5.0
      max_size_x: 10.0
      min_size_y: 5.0
      max_size_y: 10.0
      min_size_z: 2.5
      max_size_z: 4.0
      min_absorption_value: 0.05
      max_absorption_value: 0.20
      min_mic_distance: 1.0
      max_mic_distance: 3.0
      use_ray_tracing: false
      leave_length_unchanged: true
    p: 1.0
